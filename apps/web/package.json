{
  "name": "@insightflow/web",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "sh -c 'next dev --hostname ${HOST:-127.0.0.1} --port ${PORT:-3000}'",
    "build": "next build",
    "start": "sh -c 'next start --hostname ${HOST:-127.0.0.1} --port ${PORT:-3000}'"
  },
  "dependencies": {
    "next": "15.3.2",
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "sass": "1.89.0"
  },
  "devDependencies": {
    "@types/node": "22.15.21",
    "@types/react": "19.1.5",
    "@types/react-dom": "19.1.5",
    "typescript": "5.8.3"
  }
}
